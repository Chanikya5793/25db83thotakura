extends layout

block content
  h1 Costume Detail
  if (!toShow)
    .alert No costume found for the given id. Please return to the list and try again.
    .costume-actions
      a.btn.btn-secondary(href='/costumes') Back to costumes
  else
    - const costumeid = toShow._id
    - const costume_type = toShow.costume_type
    - const costumecost = toShow.cost
    - const costumesize = toShow.size
    - const costumecolor = toShow.color || '—'
    .detail-card
      h2 #{costume_type}
      .detail-row
        span.costumeAttr Costume ID
        span #{costumeid}
      .detail-row
        span.costumeAttr Size
        span #{costumesize}
      .detail-row
        span.costumeAttr Color
        span #{costumecolor}
      .detail-row
        span.costumeAttr Cost
        span $#{costumecost}
    .costume-actions
      a.btn.btn-outline(href='/costumes') Back to list
      a.btn.btn-primary(href=`/costumes/update?id=${costumeid}`) Edit
